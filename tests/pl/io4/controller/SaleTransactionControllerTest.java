package pl.io4.controller;

import org.junit.Test;
import org.junit.Before;

import static org.junit.Assert.*;

import pl.io4.controllers.SaleTransactionController;

/**
 * Created by Marcin on 28.04.2017.
 */
public class SaleTransactionControllerTest {
/*
    SaleTransactionController controller;

    @Before
    public void initBeforeEach() {
        controller = new SaleTransactionController();
    }

    @Test
    public void oneProductExistsTest() {
        try {
            controller.makeNewSale();

            String barCode = "1";
            controller.addProduct(barCode);

            int transactionItemsSize = controller.getTransactionItems().size();

            assertTrue(transactionItemsSize > 0);
        } catch(Exception exc) {

        }
    }

    @Test
    public void oneProductNotExistsTest() {
        try {
            controller.makeNewSale();

            String barCode = "999";
            controller.addProduct(barCode);

            int transactionItemsSize = controller.getTransactionItems().size();

            assertFalse(transactionItemsSize > 0);

        } catch (Exception exc) {

        }
    }
*/
}
